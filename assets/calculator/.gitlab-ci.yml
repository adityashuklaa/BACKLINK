pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r *.html public/ || true
    - cp -r index.html public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
